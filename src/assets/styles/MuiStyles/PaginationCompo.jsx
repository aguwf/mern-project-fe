import { makeStyles } from '@material-ui/core'

export default makeStyles((theme) => ({
  root: {
    padding: '1rem',
    maxWidth: '100%',
    marginBottom: '20px'
  },
  ul: {
    marginBottom: '4rem',
    height: '4rem',
    justifyContent: 'space-evenly'
  }
}))
